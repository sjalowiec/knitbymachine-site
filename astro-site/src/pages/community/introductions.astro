---
import BaseLayout from "../../layouts/BaseLayout.astro";
---

<BaseLayout 
  title="Introductions | Community | Knit by Machine" 
  description="Say hello to fellow machine knitters. Introduce yourself, share what machine you use, and tell us what you're knitting right now."
>
  <section class="introductions-page">
    <div class="introductions-content">
      <!-- Gate form (hidden after unlock) -->
      <section id="gate-block" style="display: block;">
        <div class="gate-form-container">
          <h2>Join the conversation</h2>
          <p class="gate-description">Enter your details to access the community introductions.</p>
          
          <form id="community-gate-form" class="gate-form kbm-gate-form">
            <div class="form-group">
              <label for="firstName">First Name <span class="required">*</span></label>
              <input 
                type="text" 
                id="firstName" 
                name="firstName" 
                required 
                placeholder="Your first name"
                data-testid="input-firstName"
              />
            </div>
            
            <div class="form-group">
              <label for="email">Email <span class="required">*</span></label>
              <input 
                type="email" 
                id="email" 
                name="email" 
                required 
                placeholder="your@email.com"
                data-testid="input-email"
              />
            </div>
            
            <button type="submit" class="submit-button kbm-button" data-testid="button-submit">
              Access Introductions
            </button>
            
            <p id="form-error" class="form-error" hidden></p>
          </form>
        </div>
      </section>

      <!-- Unlocked content (shown after unlock) -->
      <section id="open-content" hidden>
        <a href="/community" class="kbm-backlink" data-testid="link-back-community">
          &#8592; Back to Community
        </a>

        <h1>Introductions</h1>
        <p class="intro">Say hi, tell us what machine you use, and what you're knitting right now.</p>

        <div class="hyvor-talk-container">
          <hyvor-talk-comments 
            website-id="14706" 
            page-id="community-introductions"
            data-testid="hyvor-talk-comments"
          ></hyvor-talk-comments>
        </div>
      </section>
    </div>
  </section>
</BaseLayout>

<script>
  (function() {
    function initGate() {
      var gate = document.getElementById("gate-block");
      var open = document.getElementById("open-content");

      var unlocked = false;
      try {
        unlocked = localStorage.getItem("kbm:community-unlocked") === "1";
      } catch (e) {}

      if (unlocked) {
        if (gate) gate.setAttribute("hidden", "");
        if (open) open.removeAttribute("hidden");
      } else {
        if (gate) gate.removeAttribute("hidden");
        if (open) open.setAttribute("hidden", "");
      }

      var form = document.getElementById("community-gate-form");
      var errorEl = document.getElementById("form-error");

      if (form) {
        form.addEventListener("submit", function(e) {
          e.preventDefault();
          
          var firstNameInput = document.getElementById("firstName");
          var emailInput = document.getElementById("email");
          var firstName = firstNameInput ? firstNameInput.value.trim() : "";
          var email = emailInput ? emailInput.value.trim() : "";
          
          if (!firstName) {
            if (errorEl) {
              errorEl.textContent = "First name is required.";
              errorEl.removeAttribute("hidden");
            }
            return;
          }
          
          if (!email) {
            if (errorEl) {
              errorEl.textContent = "Email is required.";
              errorEl.removeAttribute("hidden");
            }
            return;
          }
          
          var submitBtn = form.querySelector('button[type="submit"]');
          if (submitBtn) {
            submitBtn.disabled = true;
            submitBtn.textContent = "Processing...";
          }
          
          fetch("/.netlify/functions/pp-unlock", {
            method: "POST",
            headers: {
              "Content-Type": "application/json",
            },
            body: JSON.stringify({
              firstName: firstName,
              email: email,
              projectId: "community",
            }),
          })
          .then(function(response) {
            return response.json();
          })
          .then(function(result) {
            if (result.ok) {
              localStorage.setItem("kbm:community-unlocked", "1");
              location.reload();
            } else {
              if (errorEl) {
                errorEl.textContent = result.message || "Something went wrong. Please try again.";
                errorEl.removeAttribute("hidden");
              }
              if (submitBtn) {
                submitBtn.disabled = false;
                submitBtn.textContent = "Access Introductions";
              }
            }
          })
          .catch(function(err) {
            if (errorEl) {
              errorEl.textContent = "Network error. Please check your connection and try again.";
              errorEl.removeAttribute("hidden");
            }
            if (submitBtn) {
              submitBtn.disabled = false;
              submitBtn.textContent = "Access Introductions";
            }
          });
        });
      }
    }

    if (document.readyState === 'loading') {
      document.addEventListener('DOMContentLoaded', initGate);
    } else {
      initGate();
    }
  })();
</script>

<script async src="https://talk.hyvor.com/embed/embed.js" type="module"></script>

<style>
  .introductions-page {
    padding: 3rem 1rem 4rem;
    max-width: 800px;
    margin: 0 auto;
  }

  .introductions-content {
    text-align: center;
  }

  /* Back link */
  .kbm-backlink {
    display: inline-block;
    margin-bottom: 1.5rem;
    padding: 0.5rem 1rem;
    color: var(--kbm-green);
    font-weight: 600;
    text-decoration: none;
    border: 1px solid var(--kbm-green);
    border-radius: 6px;
    transition: background 0.2s ease, color 0.2s ease;
  }

  .kbm-backlink:hover {
    background: var(--kbm-green);
    color: white;
  }

  h1 {
    font-size: var(--text-xxl);
    color: var(--kbm-green);
    margin-bottom: 0.75rem;
  }

  .intro {
    font-size: var(--text-lg);
    color: #555;
    max-width: 600px;
    margin: 0 auto 2rem;
    line-height: 1.6;
  }

  /* Gate form container */
  .gate-form-container {
    background: #fff;
    border: 1px solid #e0e0e0;
    border-radius: 12px;
    padding: 2rem;
    margin-bottom: 2.5rem;
    max-width: 400px;
    margin-left: auto;
    margin-right: auto;
  }

  .gate-form-container h2 {
    font-size: var(--text-lg);
    color: var(--kbm-green);
    margin: 0 0 0.5rem;
  }

  .gate-description {
    font-size: var(--text-sm);
    color: #666;
    margin: 0 0 1.5rem;
  }

  /* Form styles */
  .gate-form {
    display: flex;
    flex-direction: column;
    gap: 1rem;
  }

  .kbm-gate-form {
    position: relative;
    z-index: 1;
  }

  .form-group {
    display: flex;
    flex-direction: column;
    text-align: left;
  }

  .form-group label {
    font-size: var(--text-sm);
    font-weight: 600;
    color: #333;
    margin-bottom: 0.5rem;
  }

  .form-group .required {
    color: #e74c3c;
  }

  .form-group input {
    padding: 0.75rem 1rem;
    font-size: var(--text-base);
    border: 1px solid #ccc;
    border-radius: 6px;
    transition: border-color 0.2s ease;
  }

  .form-group input:focus {
    outline: none;
    border-color: var(--kbm-green);
  }

  /* Submit button */
  .submit-button,
  .kbm-button {
    position: relative;
    z-index: 1;
    opacity: 1;
    visibility: visible;
    background: var(--kbm-green);
    color: white;
    padding: 1rem;
    border: none;
    border-radius: 8px;
    font-size: var(--text-base);
    font-weight: 600;
    cursor: pointer;
    transition: background 0.2s ease, transform 0.1s ease;
    margin-top: 0.5rem;
  }

  .submit-button:hover,
  .submit-button:focus,
  .submit-button:focus-visible,
  .kbm-button:hover,
  .kbm-button:focus,
  .kbm-button:focus-visible {
    opacity: 1;
    visibility: visible;
    filter: none;
    mix-blend-mode: normal;
    background: var(--kbm-dark-green);
    color: white;
    transform: translateY(-2px);
  }

  .submit-button:active,
  .kbm-button:active {
    transform: translateY(0);
  }

  .submit-button:disabled,
  .kbm-button:disabled {
    background: #999;
    color: white;
    cursor: not-allowed;
    transform: none;
  }

  .form-error {
    color: #e74c3c;
    font-size: var(--text-sm);
    margin: 0.5rem 0 0;
  }

  /* Hyvor Talk container */
  .hyvor-talk-container {
    width: 100%;
    max-width: 800px;
    margin: 2rem auto 0;
    text-align: left;
  }

  /* Mobile responsive */
  @media (max-width: 768px) {
    .introductions-page {
      padding: 2rem 1rem 3rem;
    }

    h1 {
      font-size: var(--text-xl);
    }

    .intro {
      font-size: var(--text-base);
    }

    .gate-form-container {
      padding: 1.5rem;
    }
  }
</style>
