Update the /project-wizards page to include a “cards list” of available and coming-soon Project Wizards.

GOAL
- Keep the existing page header + intro copy exactly as-is (do not rewrite).
- Add a new section beneath the intro copy that displays 4 wizard cards in a responsive grid:
  - Desktop (lg+): 4 columns
  - Medium screens: 2 columns
  - Mobile: 1 column
- Do NOT make the first card look different than the others (no “featured”, no highlight, no special border).

WHERE
- Find the page file that renders /project-wizards (likely src/pages/project-wizards.astro or similar).
- Make changes only in this page (or a small local component imported by this page if that’s cleaner). Don’t refactor site-wide components.

CONTENT TO ADD (cards)
Add a section heading:
- “Project Wizards”

Then render 4 cards (same style as your existing cards on the site if you have a shared card component; otherwise match the current visual language: light border, rounded corners, padding, subtle shadow/hover).

Each card contains:
1) Title
2) One-line description
3) A small status pill (Available / Coming soon)
4) A button-style link (or disabled button for coming soon)

Cards (in this exact order):
1. Title: Getting Started: Readiness Check
   Description: Confirm your setup before you begin knitting.
   Status: Available
   CTA label: Start →
   CTA URL: link to the existing wizard route for slug “getting-started-readiness” (use your site’s wizard URL pattern; if unsure, search where other wizard links are formed and match that).

2. Title: Ready–Set–Knit: Scarves
   Description: Plan size, yarn, and stitch approach.
   Status: Coming soon
   CTA label: Coming soon
   CTA behavior: disabled (no navigation) OR link to “#” with aria-disabled and no click action.

3. Title: Ready–Set–Knit: Hats
   Description: Plan fit, yarn, and construction.
   Status: Coming soon
   CTA label: Coming soon
   CTA behavior: disabled.

4. Title: Ready–Set–Knit: Sweaters
   Description: Plan measurements, yarn, and structure.
   Status: Coming soon
   CTA label: Coming soon
   CTA behavior: disabled.

RESPONSIVE GRID
- Use Tailwind classes:
  - grid gap-4 (or gap-6 if that matches your spacing)
  - grid-cols-1 md:grid-cols-2 lg:grid-cols-4

STATUS PILL
- Small rounded pill, subtle (not shouting).
- Same style for all cards; only text changes (Available vs Coming soon).

BOTTOM LINE COPY
Replace this sentence (or the “handled” wording) with:
- “Each project type has its own planning path (socks, hats, blankets, sweaters).”
Keep it in the same location and style as the current line.

ACCESSIBILITY
- If “Coming soon” CTAs are disabled, use aria-disabled="true" and prevent click navigation.
- Ensure keyboard focus styles remain visible.

DONE CHECK
- Page renders with the existing intro area unchanged.
- Cards show 4 across on desktop, 2 across on medium, stacked on mobile.
- First card is NOT visually special.
- The “handled” sentence is replaced with the new wording.
