Goal: Simplify the /glossary index so each card shows only the image (optional) and the glossary tooltip text. Clicking a card opens the full glossary term page with the complete definition. Remove accordion, flip, and preview behaviors.

1) Update the glossary card markup

In the glossary list rendering loop:

Replace any accordion / flip / expandable content with:

title

optional image

tooltip text only

Example structure:

<article class="term-card" data-title="{lowercase title}">
  <a href="/glossary/{slug}" class="term-card-link">
    
    {# if image exists #}
    <div class="term-media">
      <img
        src="{image}"
        alt="{imageAlt}"
        loading="lazy"
        class="kbm-reference-image"
      />
    </div>

    <div class="term-content">
      <h2 class="term-title">{title}</h2>
      <p class="term-tooltip">{tooltip}</p>
    </div>

  </a>
</article>


Rules

Do NOT render the full description or definition on this page.

Tooltip text should come from the JSON tooltip field.

The entire card must be clickable.

2) CSS: modern, calm card styling (no accordion)

Add or adjust styles:

.term-card {
  border: 1px solid rgba(82,104,45,.18);
  border-radius: 16px;
  background: #fff;
  box-shadow: 0 8px 22px rgba(0,0,0,.06);
  overflow: hidden;
}

.term-card-link {
  display: block;
  text-decoration: none;
  color: inherit;
}

.term-media {
  border-bottom: 1px solid rgba(82,104,45,.14);
  background: #f7f7f4;
}

.term-media img {
  display: block;
  width: 100%;
  height: auto;
}

.term-content {
  padding: .9rem 1rem 1.1rem;
}

.term-title {
  margin: 0 0 .35rem;
  font-size: 1.05rem;
  font-weight: 800;
  color: var(--kbm-green,#52682d);
}

.term-tooltip {
  margin: 0;
  font-size: .95rem;
  line-height: 1.55;
  color: #5a6b47;
}


Optional hover/focus polish:

.term-card-link:hover .term-title {
  text-decoration: underline;
}
.term-card-link:focus-visible {
  outline: 3px solid rgba(199,216,164,.85);
  outline-offset: 3px;
}

3) Remove interaction JS

Remove all JS related to:

flipping

accordion expand/collapse

modal previews

Keep search and alphabet filtering logic intact.

Update selectors in the filter script:

.flip-card → .term-card

4) Add one helper line under the hero (optional but recommended)
<p class="glossary-helper">
  Tap a term to see the full explanation.
</p>


This eliminates any hesitation.