You are working in my Astro project.

Goal:
Add a reusable “Loading / Processing” indicator system that I can use across the site for content that may load slowly (iframes, embeds, or any element that loads after initial page render).

Requirements:
- Create a reusable Astro component: LoadingState.astro (spinner + generic text).
- Add global CSS for the spinner in: astro-site/src/styles/global.css
- Create a second component: LoadGate.astro that:
  - Shows LoadingState first
  - Hides it automatically when a target element finishes loading
  - Works for iframes and images (load event)
  - Also supports “manual hide” via a custom event (so we can use it with other embeds later)
- Generic copy only (not page-specific).
- Accessible (aria-busy/status).
- Respect prefers-reduced-motion.
- Do not break existing CSS.

Step 1: Create component
Path: astro-site/src/components/ui/LoadingState.astro
(Use the exact component content below.)

---
// LoadingState.astro
type Props = {
  id?: string;
  title?: string;
  message?: string;
  minHeight?: number | string;
  centered?: boolean;
  spinnerSize?: number;
  subtle?: boolean;
  className?: string;
};

const {
  id,
  title = "Processing…",
  message = "This may take a few seconds.",
  minHeight = 220,
  centered = true,
  spinnerSize = 36,
  subtle = false,
  className = "",
} = Astro.props;

const minH =
  typeof minHeight === "number" ? `${minHeight}px` : (minHeight ?? "220px");
---

<div
  id={id}
  class={`kbm-loading ${centered ? "is-centered" : ""} ${subtle ? "is-subtle" : ""} ${className}`}
  style={`min-height:${minH};`}
  role="status"
  aria-live="polite"
  aria-busy="true"
>
  <span
    class="kbm-loading__spinner"
    aria-hidden="true"
    style={`width:${spinnerSize}px;height:${spinnerSize}px;`}
  ></span>

  <div class="kbm-loading__text">
    <div class="kbm-loading__title">{title}</div>
    <div class="kbm-loading__message">{message}</div>
  </div>
</div>

Step 2: Append CSS to:
Path: astro-site/src/styles/global.css
Append this block at the end:

/* =========================================================
   KBM LoadingState (reusable, generic)
   ========================================================= */

.kbm-loading {
  display: flex;
  gap: 12px;
  color: var(--kbm-ink, #52682d);
}

.kbm-loading.is-centered {
  flex-direction: column;
  align-items: center;
  justify-content: center;
  text-align: center;
}

.kbm-loading__spinner {
  display: inline-block;
  border: 3px solid var(--kbm-surface-2, #e5e8e0);
  border-top-color: var(--kbm-ink, #52682d);
  border-radius: 999px;
  animation: kbm-spin 0.9s linear infinite;
}

.kbm-loading__title {
  font-weight: 600;
  line-height: 1.2;
}

.kbm-loading__message {
  opacity: 0.85;
  font-size: 0.95rem;
  line-height: 1.35;
  max-width: 42ch;
}

.kbm-loading.is-subtle .kbm-loading__message {
  opacity: 0.75;
}

@keyframes kbm-spin {
  to { transform: rotate(360deg); }
}

@media (prefers-reduced-motion: reduce) {
  .kbm-loading__spinner { animation: none; }
}

Step 3: Create a wrapper component that auto-hides
Path: astro-site/src/components/ui/LoadGate.astro

Behavior:
- Renders LoadingState (id="kbm-loadgate-loader") and a slot where I put the iframe/embed.
- The embed element MUST have a predictable selector (default: [data-loadgate-target]).
- On client-side: listen for:
  1) load event on the target element (iframe/img)
  2) OR custom window event: "kbm:loaded" with detail { id } to hide manually
- Also include an optional timeout (default 12000ms):
  - If it hasn’t loaded after timeout, keep spinner but change message to:
    "Still working… Thanks for your patience."

Props:
- id (string, required) used to scope events
- timeoutMs (number, default 12000)
- title/message for initial state (generic defaults)
- slowMessage override (generic default)

Implementation notes:
- Use a unique loader id like `loader-${id}` and a unique message id like `msg-${id}`.
- Use inline <script> in the component (vanilla JS) so no extra build tooling.
- When loaded: hide loader (display:none) and set aria-busy=false.

After creation, provide 2 usage examples in comments:
Example A: iframe embed with data-loadgate-target
Example B: manual trigger: window.dispatchEvent(new CustomEvent("kbm:loaded", { detail: { id: "my-thing" }}))

Step 4:
Verify build passes and there are no syntax errors.
