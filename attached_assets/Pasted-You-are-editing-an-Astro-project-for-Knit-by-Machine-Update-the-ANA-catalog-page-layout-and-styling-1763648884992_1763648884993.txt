You are editing an Astro project for Knit by Machine.
Update the ANA catalog page layout and styling.

Target file:

src/pages/ana-catalog.astro (or whatever file currently renders /ana-catalog)

1. Keep the existing hero section

Do not change the current ANA hero block at the top of the page:

Ask ’n Answer logo bubble on the left

Heading, subheading text

Search field and “Sort by” dropdown

Leave this markup and styling as-is.

Assume there is some outer wrapper like:

<section class="ana-hero">…</section>


If it doesn’t have this class yet, wrap the hero in <section class="ana-hero">…</section>.

2. Add a separate “well” under the hero for the category speech-bubble buttons

Immediately below the hero section, add a new section that contains:

A label: “Browse by topic:”

The speech-bubble category buttons

Example Astro structure (adapt to existing data):

<section class="ana-topics-well">
  <p class="ana-topics-label">Browse by topic:</p>

  <div class="ana-category-grid">
    {categories.map((cat) => (
      <button
        class={`ana-category-bubble ${selectedCategory === cat.slug ? 'is-active' : ''}`}
        on:click={() => setSelectedCategory(cat.slug)}
        type="button"
      >
        {cat.title}
      </button>
    ))}
  </div>
</section>


Notes:

categories should be whatever you already use for ANA categories (Sanity / JSON).

selectedCategory can be simple state (if you’re already filtering) or you can just leave all buttons unselected for now.

Don’t move the hero into this section; it must be a separate block below the hero.

3. Replace the raw ANA entry list with cards

Currently, the ANA entries show as basic text links (e.g., underlines, no card styling).

Change this to a card grid.

Below the topics well, render the filtered ANA items (or all items) like this:

<section class="ana-list">
  <div class="ana-card-grid">
    {anas.map((ana) => (
      <a href={`/ana/${ana.slug}`} class="ana-card">
        <h3>{ana.title}</h3>
        {ana.excerpt && <p>{ana.excerpt}</p>}
      </a>
    ))}
  </div>
</section>


Use the existing ANA data array (whatever you currently map over).

Each card links to the individual ANA page via its slug.

4. Add / update CSS for the new layout

Add the following CSS to a global stylesheet that affects /ana-catalog (for example your main CSS file, or inside this Astro file using <style is:global>).

4.1 Topics “well” styling
.ana-hero {
  /* keep existing hero styles, or leave empty if already styled */
}

.ana-topics-well {
  background-color: #ffffff;
  border-radius: 16px;
  box-shadow: 0 4px 18px rgba(82, 104, 45, 0.10);
  padding: 1.5rem 2rem 1.75rem;
  margin: 1.5rem 0 2rem;
}

.ana-topics-label {
  margin: 0 0 0.75rem;
  font-size: 0.95rem;
  font-weight: 500;
  color: #52682d;
}

4.2 Speech-bubble category grid + buttons

Use this exact CSS for the speech-bubble categories (this matches the test snippet the user provided):

/* ================================================
   ANA CATEGORY BUBBLE GRID
   ================================================ */
.ana-category-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(220px, 1fr));
  gap: 1.5rem;
  margin: 0;
  padding-bottom: 0.25rem;
}

/* ================================================
   BASE CATEGORY BUBBLE
   ================================================ */
.ana-category-bubble {
  display: inline-block;
  width: 100%;
  position: relative;

  background-color: #ffffff;
  color: #2c3a57;
  padding: 0.85rem 1.4rem;
  border-radius: 20px;
  border: 2px solid #52682d;

  font-size: 1rem;
  font-weight: 500;
  text-align: center;
  cursor: pointer;

  box-shadow: 0 2px 6px rgba(82, 104, 45, 0.12);
  transition:
    background-color 0.15s ease,
    border-color 0.15s ease,
    box-shadow 0.15s ease,
    transform 0.12s ease;
}

/* ================================================
   SPEECH BUBBLE TAIL — Bottom Right (Outer triangle)
   ================================================ */
.ana-category-bubble::after {
  content: "";
  position: absolute;
  bottom: -14px;
  right: 26px;
  width: 0;
  height: 0;
  border-left: 18px solid transparent;
  border-right: 0;
  border-top: 14px solid #52682d;
  transition: border-top-color 0.15s ease;
}

/* ================================================
   SPEECH BUBBLE TAIL FILL — Inner triangle
   ================================================ */
.ana-category-bubble::before {
  content: "";
  position: absolute;
  bottom: -11px;
  right: 28px;
  width: 0;
  height: 0;
  border-left: 15px solid transparent;
  border-right: 0;
  border-top: 11px solid #ffffff;
  z-index: 1;
  transition: border-top-color 0.15s ease;
}

/* HOVER STATE */
.ana-category-bubble:hover {
  background-color: #f7f9f3;
  border-color: #6e8b3d;
  transform: translateY(-1px);
  box-shadow: 0 3px 8px rgba(82, 104, 45, 0.20);
}

.ana-category-bubble:hover::after {
  border-top-color: #6e8b3d;
}

.ana-category-bubble:hover::before {
  border-top-color: #f7f9f3;
}

/* ACTIVE / SELECTED STATE */
.ana-category-bubble.is-active {
  background-color: #eef3e4;
  border-color: #6e8b3d;
  font-weight: 600;
}

.ana-category-bubble.is-active::after {
  border-top-color: #6e8b3d;
}

.ana-category-bubble.is-active::before {
  border-top-color: #eef3e4;
}

4.3 ANA cards styling
.ana-list {
  margin-top: 1.5rem;
}

.ana-card-grid {
  display: grid;
  grid-template-columns: repeat(auto-fit, minmax(280px, 1fr));
  gap: 1.5rem;
}

.ana-card {
  display: block;
  background: #ffffff;
  border: 1px solid #d1d9c2;
  border-radius: 12px;
  padding: 1.25rem 1.5rem;
  box-shadow: 0 2px 6px rgba(82, 104, 45, 0.08);
  text-decoration: none;
  color: #2c3a57;
  transition: transform 0.12s ease, box-shadow 0.15s ease;
}

.ana-card:hover {
  transform: translateY(-3px);
  box-shadow: 0 4px 12px rgba(82, 104, 45, 0.15);
}

.ana-card h3 {
  margin-top: 0;
  margin-bottom: 0.5rem;
  font-size: 1.1rem;
  color: #2c3a57;
}

.ana-card p {
  margin: 0;
  color: #5a5a5a;
  font-size: 0.95rem;
}

5. Responsiveness

Ensure the layout remains responsive:

On smaller screens, the hero stays full-width on top.

The topics well sits directly below the hero with full width and single-column bubble grid if needed.

The ANA cards stack gracefully (1 per row on narrow screens, 2+ per row on wider screens).

Use the existing KBM responsive breakpoints; don’t introduce new ones unless necessary.