You are editing my Astro + Tailwind project.

GOAL
Implement the full Getting Started page logic:

1. The “I’m ready for my next step” button is hidden on page load.
2. MindPal sends a postMessage event "mindpal-chat-completed" when the agent finishes.
3. When that message is received:
   • Reveal the button by removing the "hidden" class.
4. When the user clicks the button:
   • Trigger confetti (uses existing window.triggerConfetti if available, otherwise fallback).
   • Smooth-scroll to the “What’s Next?” section on the same page.

Please make all the following updates.

------------------------------------------------------------
STEP 1 — OPEN THE GETTING STARTED PAGE
Located at one of these paths (whichever exists in my project):
- src/pages/getting-started.astro
- src/pages/learn/getting-started.astro
Use the correct file.

------------------------------------------------------------
STEP 2 — ADD/CONFIRM THE HIDDEN BUTTON BELOW THE CHATBOT

Right below my chatbot embed, insert this EXACT block if it's not already there:

<div class="mt-8 flex justify-center">
  <button
    id="getting-started-complete-btn"
    class="hidden px-6 py-3 rounded-full text-base font-medium bg-kbm-green text-white shadow-md hover:shadow-lg transition focus:outline-none focus:ring-2 focus:ring-offset-2 focus:ring-kbm-green"
    type="button"
    onclick="handleGettingStartedComplete()"
  >
    I’m ready for my next step
  </button>
</div>

Use bg-kbm-green or my closest existing green utility class.

------------------------------------------------------------
STEP 3 — ENSURE THE “WHAT’S NEXT?” SECTION EXISTS

Add this (or confirm it's already present) BELOW the button:

<section
  id="whats-next"
  class="mt-16 border-t border-slate-200 pt-10"
>
  <h2 class="text-2xl font-semibold text-slate-900 mb-3">
    What’s next with your machine?
  </h2>
  <p class="text-slate-600 mb-6 max-w-xl">
    You’ve taken a solid first step. From here, you can get quick answers, build skills, or reach out for more help.
  </p>

  <div class="grid gap-4 md:grid-cols-3">
    <a
      href="/learn/ask-n-answer"
      class="block rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:shadow-md transition"
    >
      <h3 class="text-lg font-semibold mb-1">Get quick answers (ANA)</h3>
      <p class="text-sm text-slate-600">
        Browse clear answers to common machine knitting questions.
      </p>
    </a>

    <a
      href="/knit/skill-builders"
      class="block rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:shadow-md transition"
    >
      <h3 class="text-lg font-semibold mb-1">Build skills (Skill Builders)</h3>
      <p class="text-sm text-slate-600">
        Start a small practice exercise to get more confident with your machine.
      </p>
    </a>

    <a
      href="/contact"
      class="block rounded-xl border border-slate-200 bg-white p-4 shadow-sm hover:shadow-md transition"
    >
      <h3 class="text-lg font-semibold mb-1">Need more help? (Contact Sue)</h3>
      <p class="text-sm text-slate-600">
        Have something more specific? Send Sue a note on the contact page.
      </p>
    </a>
  </div>
</section>

------------------------------------------------------------
STEP 4 — REPLACE THE <script> BLOCK WITH THIS EXACT ONE

At the bottom of the same Astro page (not inside frontmatter), replace the entire existing <script> tag with:

<script>
  // -------------------------------------------
  // Confetti fallback (if no existing confetti lib)
  function triggerConfettiFallback() {
    document.body.animate(
      [
        { transform: 'translateY(0px)' },
        { transform: 'translateY(-10px)' },
        { transform: 'translateY(0px)' },
      ],
      { duration: 300, iterations: 1 }
    );
  }

  // -------------------------------------------
  // Reveal button, trigger confetti, scroll
  function handleGettingStartedComplete() {
    // Confetti
    if (typeof window.triggerConfetti === "function") {
      window.triggerConfetti();
    } else {
      triggerConfettiFallback();
    }

    // Scroll to “What’s Next?”
    const section = document.getElementById("whats-next");
    if (section) {
      section.scrollIntoView({ behavior: "smooth", block: "start" });
    }
  }
  window.handleGettingStartedComplete = handleGettingStartedComplete;

  // -------------------------------------------
  // Show the hidden button
  window.showGettingStartedCompleteButton = function () {
    const btn = document.getElementById("getting-started-complete-btn");
    if (btn) btn.classList.remove("hidden");
  };

  // -------------------------------------------
  // MINDPAL COMPLETION HOOK
  // Listen for postMessage event: "mindpal-chat-completed"
  window.addEventListener("message", (event) => {
    if (event.data === "mindpal-chat-completed") {
      window.showGettingStartedCompleteButton?.();
    }
  });
</script>

------------------------------------------------------------
STEP 5 — VALIDATE

Ensure:
- The page compiles without errors.
- Button is hidden on load.
- When MindPal chat completes, the button appears.
- Clicking triggers confetti + smooth scroll.

END OF INSTRUCTIONS
